# Laravel Multi-Site Development Environment Configuration

# User ID for file permissions (Linux only, defaults to 1000)
# Run `id -u` to find your UID on Ubuntu/Linux
# UID=1000

# Application Configuration (for default database)
APP_ENV=local
APP_DEBUG=true

# MySQL Root Configuration
# The setup script will generate secure passwords automatically
# You can also generate your own: openssl rand -base64 32
MYSQL_ROOT_PASSWORD=ChangeThisToASecurePassword123!
MYSQL_DATABASE=default_db
MYSQL_USER=default_user
MYSQL_PASSWORD=ChangeThisToASecurePassword456!

# MySQL Performance Tuning (Optimized for 4GB Server)
# Adjust based on your system RAM - these settings use ~2.5GB
MYSQL_INNODB_BUFFER_POOL_SIZE=2G
MYSQL_INNODB_LOG_FILE_SIZE=512M
MYSQL_INNODB_FLUSH_LOG_AT_TRX_COMMIT=2
MYSQL_MAX_CONNECTIONS=200
MYSQL_TABLE_OPEN_CACHE=4000
MYSQL_SORT_BUFFER_SIZE=4M
MYSQL_READ_BUFFER_SIZE=2M
MYSQL_JOIN_BUFFER_SIZE=2M
MYSQL_TMP_TABLE_SIZE=128M
MYSQL_MAX_HEAP_TABLE_SIZE=128M

# Note: Each site will have its own database created by the setup script
# Site-specific credentials are stored in credentials/{sitename}.env
PHP_UPLOAD_MAX_FILESIZE=100M
PHP_POST_MAX_SIZE=100M

# Timezone
TZ=UTC
